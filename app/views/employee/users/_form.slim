= form_for [:employee, @user], local: true do |f|

  .field
    = f.label :first_name, class: "label"
    .control
      = f.text_field :first_name, class: "input"

  .field
    = f.label :last_name, class: "label"
    .control
      = f.text_field :last_name, class: "input"

  .field
    = f.label :email, class: "label"
    .control
      = f.email_field :email, class: "input"

  .field
    = f.label :mobile_phone, class: "label"
    .control
      = f.text_field :mobile_phone, placeholder: "+7 (707) 341-59-52", class: "input phone", data: {phone: true}

  .field
    = f.label :password, class: "label"
    .control
      = f.password_field :password, class: "input"
  
  / .select.mb-4
  .field
    = f.label :role_id, class: "label"
    .control.select
      = f.collection_select :role_id, Role.with_individual(@individual).all.order(:title), :id, :title, {}, class: "input"

  .field.is-grouped
    .control
      = f.submit "Submit", class: "button is-primary"